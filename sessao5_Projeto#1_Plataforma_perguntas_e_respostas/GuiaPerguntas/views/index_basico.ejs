<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Guia perguntas</title>
</head>
<body>
    <h1>Guia de Perguntas</h1>
    <hr>
    <h3>Listas de perguntas</h3>
    <p>Nome :<%= nome %></p>
    <p>Linguagem :<%= lang %></p>
    <p>Empresa :<%= empresa %></p>
    <p>Inscritos :<%= inscritos %></p>
    <hr>
    
    <% if(msg){ %>
        <h3>Isso é uma mensagem de erro</h3>
    <% } else{ %>
        <h3>Nenhum erro</h3>
    <% } %>

    <!--Estrutura de repetição do foreach no JS-->
    <% if(produtos) { %>
        <table class="table">
            <thead>
                <th>Nome</th>
                <th>Preço</th>
            </thead>
            <tbody>
                <% produtos.forEach(function(produto){ %>
                    <tr>
                        <td><%= produto.nome %></td>
                        <td><%= produto.preco %> </td>
                    </tr>           
                <% } ) %>
            </tbody>
        </table>
    <% } %>

    <img src="/img/xiomi.jpg"
     
</body>
</html>